services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: absalom-command-center-api
    ports:
      - "8000:8000"
    environment:
      - PYTHONPATH=/app
      - ENVIRONMENT=development
